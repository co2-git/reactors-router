{
  "name": "reactors-router",
  "version": "0.0.4",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "babel": "babel --presets es2015,react,stage-0 --out-dir dist/ src/",
    "babel-watch": "babel --presets es2015,react,stage-0 --watch --out-dir dist/ src/"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.5.0",
    "babel-preset-stage-0": "^6.5.0"
  },
  "dependencies": {
    "lodash": "^4.13.1"
  }
}
